package act7_Henry;

public class whiteBoard {
    int addNumbers(int addend1, int addend2) {
        int sum = addend1 + addend2; // compute the sum
        return sum; // return the result
    }
}
